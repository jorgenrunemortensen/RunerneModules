namespace Runerne.Utilities
{
    /// <summary>
    /// Defines the fields contained in a <see cref="System.DateTime"/> type.
    /// </summary>
    public enum DateTimePart
    {
        Milliseconds,
        Seconds,
        Minutes,
        Hours,
        Days,
        Weeks,
        Months,
        Years,
    }
}
